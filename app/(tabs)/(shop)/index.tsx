import { ThemedView } from '@/components/ThemedView';
import React from 'react';
import { View, Text } from 'react-native';

export default function ShopScren(){

    return (
        <ThemedView style={{flex: 1, justifyContent: "center", alignContent: "center"}}>
            <Text>Shop</Text>
        </ThemedView>
    )

}